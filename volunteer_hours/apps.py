from django.apps import AppConfig


class VolunteerHoursConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'volunteer_hours'
    verbose_name = 'النقاط والساعات التطوعية'
